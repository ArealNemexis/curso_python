# tupla, uma estrutura de dados linerar, que uma vez declarada não pode ser alterada
# IMUTAVEL
# aceita indexamento numerico
# geralmente utilizada para bancos de dados ou constantes

tupla = (23, 25, 36)

#print(tupla[0])

# tupla[0] = 35 erro

for item in tupla:
    print(item)